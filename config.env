# =============================================================================
# Travel Router Configuration
# Edit this file before running setup.sh
# =============================================================================

# -----------------------------------------------------------------------------
# Personal Hotspot (Access Point) Settings
# These define the WiFi network YOUR devices connect to
# -----------------------------------------------------------------------------

AP_SSID="MyTravelPi"
AP_PASSWORD="ChangeMe123!"     # Min 8 characters for WPA2

# Country code for regulatory WiFi compliance (ISO 3166-1 alpha-2)
# This should match the country you're in, or your home country
AP_COUNTRY_CODE="US"

# WiFi band: "bg" (2.4 GHz only) or "a" (5 GHz only)
# 2.4 GHz (bg) has better range and broader compatibility; use this unless
# you have a specific reason to need 5 GHz
AP_BAND="bg"

# Channel for 2.4 GHz: 1, 6, or 11 (non-overlapping channels)
# Channel for 5 GHz: 36, 40, 44, 48, 149, 153, 157, 161
AP_CHANNEL=6

# -----------------------------------------------------------------------------
# Network Addressing
# IP range that will be assigned to devices connecting to your hotspot
# -----------------------------------------------------------------------------

AP_IP="192.168.10.1"
AP_SUBNET="192.168.10.0/24"
DHCP_RANGE_START="192.168.10.100"
DHCP_RANGE_END="192.168.10.200"
DHCP_LEASE_TIME="24h"

# -----------------------------------------------------------------------------
# Interface Names
# Verify these match your hardware before running setup.sh
#
# To list interfaces: ip link show
# Built-in WiFi is usually wlan0
# USB WiFi adapter is usually wlan1 (but check — it could be wlan0 on some systems)
# -----------------------------------------------------------------------------

# Interface that creates YOUR personal hotspot (built-in WiFi)
AP_INTERFACE="wlan0"

# Interface that connects to hotel/venue WiFi (USB WiFi adapter)
# Change to "eth0" if you want to use an Ethernet cable as WAN instead
WAN_INTERFACE="wlan1"

# -----------------------------------------------------------------------------
# NordVPN Settings
# -----------------------------------------------------------------------------

# Default country for VPN connection
# Use country name (e.g., "United_States", "Germany", "Netherlands")
# Or a city (e.g., "New_York", "Amsterdam")
# Run `nordvpn countries` on the Pi to see all options
NORDVPN_COUNTRY="United_States"

# VPN protocol
# "nordlynx"    — WireGuard-based, faster, recommended
# "openvpn_udp" — OpenVPN over UDP, more compatible
# "openvpn_tcp" — OpenVPN over TCP, for restrictive networks
NORDVPN_TECHNOLOGY="nordlynx"

# -----------------------------------------------------------------------------
# SSH Hardening (applied during setup)
# -----------------------------------------------------------------------------

# SSH port (default 22; change to something non-standard to reduce scan noise)
SSH_PORT=22

# Disable password login after setup (recommended: "yes")
# Requires that your SSH public key is already in authorized_keys before enabling
SSH_DISABLE_PASSWORD="yes"
